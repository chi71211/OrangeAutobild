@echo off
chcp 65001 >nul
title RDKS 自動更新排程器

:: 切換到此批次檔所在的目錄 (請確保此檔與 Python 腳本放在同一個資料夾)
cd /d "%~dp0"

:: 檢查 Python 是否已安裝
python --version >nul 2>&1
if %errorlevel% neq 0 (
    echo [錯誤] 找不到 Python！請確認您的電腦已經安裝 Python。
    pause
    exit /b
)

:: 啟動 Python 腳本 (Python 腳本內會自己判斷這個月是否需要抓取)
python 胎壓檢測器_開機自動版.py

exit /b
```eof
